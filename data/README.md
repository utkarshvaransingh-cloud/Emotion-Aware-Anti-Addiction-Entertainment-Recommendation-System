Data folder layout

- `data/raw/` — raw ingested files (CSV, JSON, API dumps)
- `data/processed/` — cleaned and featureified parquet files
- `data/samples/` — small sample datasets for demos and tests

Place collected raw files under `data/raw/<source>/` with a timestamped filename.
