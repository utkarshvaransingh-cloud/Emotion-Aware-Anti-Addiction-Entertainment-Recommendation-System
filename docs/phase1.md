# Phase 1 — Project skeleton & README

What I created:
- `README.md` — project description and phases
- `requirements.txt` — minimal dependencies
- `.gitignore` — common Python ignores
- `src/main.py` — minimal FastAPI app with `GET /health` and `POST /recommend`

Next steps for Phase 2 (I can start when you confirm):
- List data sources and dataset format
- Create data collection scripts and storage layout
- Lock dependencies and create a virtualenv/devcontainer
